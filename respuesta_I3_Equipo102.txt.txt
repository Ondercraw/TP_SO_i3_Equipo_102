a- La rama master queda actualizada con los datos de la rama "Velazco"
b- Si, beta pudo ver todos los cambios que se realizaron
c- Beta debio hacer los siguientes comandos:

$ git checkout main
$ git merge Romero
$ git add .
$ git commit -m ""
$ git push

d-Las ramas no se actualizan, sin que el usuario lo modifique
e-No tuvimos problemas
f- Gamma debio hacer los siguientes comandos:

$ git clone --branch Velazco https://github.com/Ondercraw/TP_SO_i3_Equipo_102.git
$ (echo -e "Melany Romero" && ipconfig) >> beta.txt
$ git add .
$ git commit -m ""
$ git push

g- ALFA uso los sig. comandos:

$ git clone --branch Velazco https://github.com/Ondercraw/TP_SO_i3_Equipo_102.git
$ ipconfig >> beta.txt
$ git status
$ git add .
$ git commit -m "ALFA realizo interaccion 8"
$ git push